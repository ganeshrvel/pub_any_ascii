name: any_ascii
description: Unicode to ASCII transliteration for Dart, ported from the anyascii project.
version: 1.0.0
homepage: https://github.com/ganeshrvel/pub_any_ascii

environment:
  sdk: ">=3.10.0 <4.0.0"

dev_dependencies:
  very_good_analysis: ^10.2.0
  test: ^1.30.0
